XKB_DEFAULT_LAYOUT=ch,us
XKB_DEFAULT_OPTIONS=grp:alt_shift_toggle,grp_led:scroll
XCURSOR_SIZE=24
XCURSOR_THEME=Adwaita
SSH_AUTH_SOCK=/run/user/1000/keyring/ssh

# $DESKTOP_SESSION not set, ignoring.
# $I3SOCK not set, ignoring.
# $XCURSOR_THEME not set, ignoring.
# $XCURSOR_SIZE not set, ignoring.
# $SSH_AUTH_SOCK not set, ignoring.

# little trick to get the current layout is to use the scroll lock LED. in fedora is input 4
# if [ "$(cat /sys/class/leds/input4\:\:scrolllock/brightness)" == "1" ]; then echo 'US';else echo 'CH'; fi